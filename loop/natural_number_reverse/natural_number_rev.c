#include <stdio.h>
int main()
{
    int n = 100, i;
    /* printf("Enter the number");
    scanf("%d", &n); */
    for (i = n; i >= 0; i--)
    {
        printf("\n i = %d", i);
    }

    return 0;
}