#include <stdio.h>
int main()
{
    int n=20;
    // printf("please enter the value :");
    // scanf("%d", &n);
    if (n % 2 != 0)
    {
        printf("%d is a Odd number \n", n);
    }
    else
    {
        printf("%d is a Even number \n", n);
    }
    return 0;
}